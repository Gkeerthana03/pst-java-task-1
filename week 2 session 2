Given an integer array `nums` and an integer `val`, remove all occurrences of `val` in `nums` [**in-place**](https://en.wikipedia.org/wiki/In-place_algorithm). The order of the elements may be changed. Then return *the number of elements in *`nums`* which are not equal to *`val`.

Consider the number of elements in `nums` which are not equal to `val` be `k`, to get accepted, you need to do the following things:

- Change the array `nums` such that the first `k` elements of `nums` contain the elements which are not equal to `val`. The remaining elements of `nums` are not important as well as the size of `nums`.
- Return `k`.

**Custom Judge:**

The judge will test your solution with the following code:

```
int[] nums = [...]; // Input array
int val = ...; // Value to remove
int[] expectedNums = [...]; // The expected answer with correct length.
                            // It is sorted with no values equaling val.

int k = removeElement(nums, val); // Calls your implementation

assert k == expectedNums.length;
sort(nums, 0, k); // Sort the first k elements of nums
for (int i = 0; i < actualLength; i++) {
    assert nums[i] == expectedNums[i];
}

```

If all assertions pass, then your solution will be **accepted**.

**Example 1:**

```
Input: nums = [3,2,2,3], val = 3
Output: 2, nums = [2,2,_,_]
Explanation: Your function should return k = 2, with the first two elements of nums being 2.
It does not matter what you leave beyond the returned k (hence they are underscores).

```

**Example 2:**

```
Input: nums = [0,1,2,2,3,0,4,2], val = 2
Output: 5, nums = [0,1,4,0,3,_,_,_]
Explanation: Your function should return k = 5, with the first five elements of nums containing 0, 0, 1, 3, and 4.
Note that the five elements can be returned in any order.
It does not matter what you leave beyond the returned k (hence they are underscores).

```

**Constraints:**

- `0 <= nums.length <= 100`
- `0 <= nums[i] <= 50`
- `0 <= val <= 100`

[Advertisement](https://googleads.g.doubleclick.net/pagead/ads?client=ca-pub-3965754337269139\&output=html\&h=60\&slotname=9355766793\&adk=2106994225\&adf=642336653\&pi=t.ma~as.9355766793\&abgtt=6\&w=468\&lmt=1787379414\&format=468x60\&url=https%3A%2F%2Fleetcode.com%2Fproblems%2Fremove-element%2Fdescription%2F\&aiof=13\&asro=0\&aimartd=4\&aieuf=1\&aicrs=1\&uach=WyJXaW5kb3dzIiwiMTQuMC4wIiwieDg2IiwiIiwiMTUxLjAuNzkyMi4xNzMiLG51bGwsMCxudWxsLCI2NCIsW1siTm90PUE_QnJhbmQiLCI5OS4wLjAuMCJdLFsiR29vZ2xlIENocm9tZSIsIjE1MS4wLjc5MjIuMTczIl0sWyJDaHJvbWl1bSIsIjE1MS4wLjc5MjIuMTczIl1dLDBd\&dt=1787379414812\&bpp=1\&bdt=3067\&idt=1\&shv=r20260820\&mjsv=m202608190101\&ptt=9\&saldr=aa\&abxe=1\&cookie=ID%3D8e6c1922292bb909%3AT%3D1784627972%3ART%3D1787378055%3AS%3DALNI_Mbc1zxsUvQf6-R7G1H56WZ486WjNg\&gpic=UID%3D000014cf433da6d8%3AT%3D1784627972%3ART%3D1787378055%3AS%3DALNI_MZyfXall-E6f6NfugC7Oe2j0mUHVw\&eo_id_str=ID%3Dc2e9b911b08ca7e7%3AT%3D1784627972%3ART%3D1787378055%3AS%3DAA-AfjZZpQ43QdfnvmjYZt1NJxGB\&prev_fmts=0x0\&nras=1\&correlator=8601366876654\&frm=20\&pv=1\&u_tz=330\&u_his=2\&u_h=1080\&u_w=1920\&u_ah=1032\&u_aw=1920\&u_cd=24\&u_sd=1\&dmc=32\&adx=26\&ady=293\&biw=1920\&bih=945\&scr_x=0\&scr_y=0\&eid=95396000%2C95397679%2C95399755%2C122880564\&oid=2\&pvsid=6728490569881102\&tmod=352363535\&uas=3\&nvt=1\&fc=1920\&brdim=0%2C0%2C0%2C0%2C1920%2C0%2C1920%2C1032%2C1920%2C945\&vis=1\&rsz=o%7Co%7CaeoE%7C\&abl=NA\&pfx=0\&fu=0\&bc=31\&bz=1\&pgls=CAk.\&ifi=2\&uci=a!2\&fsb=1\&dtd=4)

svg givethe java code correctly
class Solution {

    public int removeElement(int[] nums, int val) {

        int k = 0;

        for (int i = 0; i < nums.length; i++) {

            if (nums[i] != val) {
                nums[k] = nums[i];
                k++;
            }
        }

        return k;
    }
}
